la command :
```shell
bun run dev

```
doit faire :
 
```shell
"bun run db:dev:start"
```
si ce n'est pas deja fait

ensuite
```shell
bun run db:dev:push
```
pour push le schema
si ce n'est pas deja fait
ensuite
```shell
bun run db:dev:seed
```
pour seed la db
si ce n'est pas deja fait
puis
 
```shell
bun run dev:backend
```
puis
 
```shell
bun run dev:frontend
```
comme ca je n'ai pas de lancer plusieurs fenetre de terminal pour lancer le projet
je ne suis pas sur si le shell inclut dans le runtime bun peut etre utile dans ce contexte 
aide moi

